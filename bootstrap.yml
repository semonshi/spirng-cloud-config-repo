spring:
  application:
    name: configserver
  cloud:
    zookeeper:
      connect-string: zoo1:2181,zoo2:2181,zoo3:2181
      config:
        enabled: true
        root: config
        defaultContext: applications
        profileSeparator: ','
      discovery:
        enabled: true
        root: /scz